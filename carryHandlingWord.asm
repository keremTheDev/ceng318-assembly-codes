.MODEL SMALL
.STACK 64

.DATA
DATA_IN DW 27345,28521,29533,30105,32375
ORG 0010H
SUM DW 2 DUP(?)

.CODE
MAIN PROC FAR
    MOV AX,@DATA
    MOV DS,AX

    MOV CX,5
    MOV SI,OFFSET DATA_IN
    XOR AX, AX
    XOR BX, BX

BACK:
    ADD AX, [SI]
    ADC BX, 0
    ADD SI, 2
    LOOP BACK

    MOV DI, OFFSET SUM
    MOV [DI], AX
    MOV [DI+2], BX

    MOV AH, 4CH
    INT 21H
MAIN ENDP
END MAIN
